# Welcome to model_learning_monitoring project

The project is deployed in 3 docker containers: Prometheus, Grafana, model_train.   Docker is required to run the project.  
  
# Project launch 
```
docker-compose up --build
```